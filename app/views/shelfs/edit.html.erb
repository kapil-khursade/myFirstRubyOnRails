<h1>Edit Shelf</h1>

<%= form_with(model: @shelf, local: true) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit %>
<% end %>

<%= link_to 'Back', sections_path %>