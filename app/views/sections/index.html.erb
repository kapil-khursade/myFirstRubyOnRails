<h1>Sections</h1>

<ol>
  <% @sections.each do |section| %>
    <li><%= link_to section.name, section_path(section) %></li>
  <% end %>
</ol>

 <li><%= link_to "+ Add New Section", new_section_path() %></li>

